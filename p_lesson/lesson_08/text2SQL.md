## 自助式数据报表开发

### Text to SQL
* Text2SQL 是一种自然语言处理技术，它能够将用户的自然语言问题直接转换为 SQL 查询语句。用户不需要懂 SQL 语法，只需要用自然语言提问，系统就能自动生成对应的数据库查询。
### LLM模型选择
### Function Call
### 搭建SQL copilot
### Langchain中的SQL agent
### 自己编写LLM+Prompt
* case 保险场景SQL copilot实战

### FAQ
#### 可不可以用知识库来限定使用的表结构？
* 一般知识库是用于保存之前查询问题的SQL
* RAG的作用是开卷考试，做一个相似度的检索